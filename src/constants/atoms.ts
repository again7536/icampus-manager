const ATOM_KEYS = {
  PLAYLIST: "playList",
  SELECTED_COURSE: "selectedCourse",
  SETTINGS: "settings",
  VERSION: "version",
  CUSTOM_ASSIGNMENT: "customAssignment",
} as const;

export { ATOM_KEYS };
