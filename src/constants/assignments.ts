import { LectureType } from "@/types";

const LECTURE_TYPE: { [key: string]: LectureType } = {
  MOVIE: "movie",
  ZOOM: "zoom",
  EVERLEC: "everlec",
  MP4: "mp4",
  SCREEN_LECTURE: "screenlecture",
};

export { LECTURE_TYPE };
